package user;

/**
 * @author Geoffrey Long
 * 
 * Provides a way to easily differentiate between logged in and not logged in players
 */
public class NullPlayer extends Player{

	public NullPlayer(int playerNumber) {
		super(playerNumber);
	}

}
